<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- CSV 导出文件缓存目录（ADR 0008） -->
    <cache-path name="exports" path="exports/" />
    <!-- 相机拍照临时图片（OCR 拍照记账） -->
    <cache-path name="camera" path="camera/" />
    <!-- 从系统图片选择器导入的 OCR 临时图片 -->
    <cache-path name="ocr" path="ocr/" />
</paths>
