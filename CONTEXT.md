# BeeCount

个人记账应用。核心功能是 AI 小助手：用户用一句自然语言描述一笔收支（如"昨天打车花了30块"），AI 解析出结构化账目，用户确认后入库。

## 语言

**账目 (Entry)**:
一条已入库的收支记录，包含类型（支出/收入）、金额、类别、日期、备注与可选的交易对方（商家/转账方）。交易对方姓名是独立的 `counterparty` 字段，微信账单导入时来自账单行，手动/AI/OCR 记账可识别时写入，无法识别时为 null；账本页可按对方筛选、关键词搜索也覆盖它。
_Avoid_: 流水、transaction、record

**支出 (Expense)**:
账目的类型之一，表示钱的流出，如"打车花了30块"。
_Avoid_: 消费、花费

**收入 (Income)**:
账目的类型之一，表示钱的流入，如"老板发了500红包"。
_Avoid_: 入账、进账

**类别 (Category)**:
账目的分类标签，由预定义类别（餐饮、交通、购物…）和用户自定义类别共同构成，支持二级层级（一级分类下设子分类）；AI 解析时从完整类别表中选择。每个类别可有 Emoji 图标与颜色标签；支持重命名、按使用频率排序（可拖拽覆盖）、隐藏不常用、以及删除时把历史账目归并到目标分类。账目存的是类别名快照：一级分类快照是类别名，子分类快照是「父·子」完整路径。用户可以在确认账目时临时创建新类别。
_Avoid_: 标签、分类名

**解析 (Parse)**:
AI 将用户原话转换为结构化账目的过程，输出为 JSON。解析出的账目只是草稿，需经用户确认才入库。
_Avoid_: 识别、提取

**确认卡片 (Confirmation Card)**:
解析结果的呈现形式，用户在此核对并确认账目。卡片上金额与类别可编辑，日期与备注只读。确认后账目入库。
_Avoid_: 预览卡片、草稿

**可记账输入 (Recordable Input)**:
能够被解析成账目的用户原话。无法解析为账目的输入（闲聊、查询）不会被生成卡片，而是以自然语言消息回应。
_Avoid_: 有效输入

**今日已记 (Today's Entries)**:
首页常驻的当日账目列表，按日期只展示当天入库的账目，不含管理操作。
_Avoid_: 流水页

**今日合计 (Today's Totals)**:
今日已记列表顶部的当日支出与收入汇总，随入库实时更新。
_Avoid_: 统计、汇总行

**撤销 (Undo)**:
确认入库后短时间内删除该账目的操作，用于纠正误确认。
_Avoid_: 删除、回滚

**查询模块 (EntryQuery)**:
`domain/query` 包中的深模块，把"给定日期/区间/粒度 → 聚合结果"的查询语义集中在一个接口后。与 `domain/usecase` 的动作类（解析、确认、删除）形成对照：查询（observe/build/aggregate）与动作（command）是深 module 的自然分界。注入 `EntryRepository` / `CategoryRepository` / `TagRepository` / `BudgetRepository`，接口为 method-based（约 10 个方法），实现吸收原先转发与 map 逻辑；观察数据与构建月/年/日历聚合都在此，`BuildComparisonAnalyticsUseCase`（真实周期边界数学 + combine）保留为独立深模块。
_Avoid_: 服务、查询仓库、gateway

**记账入口 (EntryIntake)**:
`domain/usecase` 包中的深模块，把"用户原话 → 解析 → 确认/编辑 → 入库"的写入规则集中在一个接口后。与 `EntryQuery`（只读查询）对照，`EntryIntake` 承担所有写动作——`parse`（原 `ParseEntryUseCase`）、`confirm`（原 `ConfirmEntryUseCase`）、`update`（原 `UpdateEntryUseCase`）、`parseOcr`（原 `OcrEntryUseCase`）。统一「库外类别先建（含不含分隔符校验）+ 库外标签注册取色 + 同事务写入」这一条领域规则，消除原先三处重复的标签取色与类别名校验。OCR 通过 `OcrTextRecognizer` seam 与 `OcrImageSource`（中立的缓存路径）接入，接口不泄漏 `android.net.Uri`。
_Avoid_: 记账服务、账目写入器

## 标签与账本

**标签 (Tag)**:
账目的附加分类标记，一笔记账可带多个标签，每个标签有唯一颜色。与类别的区别：类别每笔只有一个、决定记账主分类；标签是横向打标，用于跨类别筛选。
_Avoid_: 标记、tag 混用类别

**标签色 (Tag Color)**:
标签的唯一颜色标记。颜色存在标签库、由账目-标签关联引用，改色全局生效——所有带该标签的账目同步变色，不逐笔快照。
_Avoid_: 标签快照色

**账本页 (Ledger)**:
全部账目的历史列表页，按天分组、每天显示小计，顶部可多选标签交集筛选，并可展开综合筛选面板（关键词模糊搜索「备注/交易对方/类别/金额原文」、日期范围、分类、金额区间、交易对方、收支类型），多条件为「与」组合；筛选时显示结果合计。是 ADR 0002 排除范围的刻意扩展（见 ADR 0006）。
_Avoid_: 全部流水、明细页

**标签筛选 (Tag Filter)**:
账本页顶部的多选标签操作，多选为交集——只显示同时带所有选中标签的账目。筛选状态不持久化，离开页面即清空。
_Avoid_: 筛选器、过滤

## 预算

**预算 (Budget)**:
为总支出或某个一级分类设定的周期性金额上限，作为消费规划。一条预算 = 周期类型（周/月/季/年/自定义天数）× 维度（总预算或一分类）× 金额；总预算与各分类预算是互相独立的行，可同时存在多条。设置在设置页「管理预算」，实时进度也展示在首页概览卡。
_Avoid_: 限额、额度计划

**预算周期 (Budget Cycle)**:
预算的时间粒度，决定进度按哪个区间统计：周（周一起）、月、季、年、自定义 N 天。自定义周期以创建日为锚点分摊。
_Avoid_: 周期单位

**预算消费 (Budget Spend)**:
预算当前周期内命中的账目支出累计。口径只统计支出、排除中性与收入；总预算命中所有支出，分类预算按类别名快照「一级分类 · 任意」前缀匹配，覆盖该分类及其子分类。
_Avoid_: 已用额度、消耗

**预算结转 (Budget Carry-over)**:
把上一周期「基础额 − 支出」的正向结余滚入当前周期，作为可用额度；上期超支抹平、不反向扣当前周期。只结转最近一期。关闭结转的预算不滚入。
_Avoid_: 滚存、额度继承

**预算例外日 (Budget Exception):**
某天标为预算例外后，该日全部支出不计入任何预算消费（如旅行、搬家等集中消费日）。
_Avoid_: 豁免、跳过日

**剩余日均 (Daily Allowance):**
当前周期可花额度（基础额+结转 − 已花）除以剩余天数（含今天），供按天把握消费节奏；超支时为 0。
_Avoid_: 日均预算、可花天额

**超支预警 (Over-budget Warning):**
进度可视化语义：<90% 正常色、≥90% 橙色、≥100% 红色加粗并提示「已超支」。今日页与预算管理页同一口径。
_Avoid_: 超标、爆预算

## 设置

**设置页 (Settings)**:
第三个底部 tab，承载 API Key 管理、类别/标签管理入口、数据管理（导出/清空）与关于信息。
_Avoid_: 设置弹窗、配置页

**CSV 导出 (CSV Export)**:
账目以 CSV 文本生成并通过系统分享保存，字段含日期、类型、类别、标签、金额、备注。用于备份与外部查看。
_Avoid_: 导出文件、备份包

## 日历

**日历页 (Calendar)**:
默认底部 tab，以周一开始的月视图浏览全部账目与补记。日期格只展示日期号、紧凑支出金额和收入绿点；空日可点，未来日期与相邻月日期不可点。点按日期打开当天流水抽屉，可新增记账或删除并在五秒内撤销。
_Avoid_: 月历、日程表

**补记 (Supplementary Entry)**:
从日历选定历史或当前日期后发起的记账。今日页仍展示真实今天的概览，但输入区和确认卡片使用选定日期；确认成功后返回原日历日期。
_Avoid_: 追记、补账


**图表页 (Analytics)**:
从日历页顶部进入的独立分析详情页，默认沿用当前日历月份展示月度报告；可切换到年度报告后查看趋势、高亮和只读热力图。
_Avoid_: 第四个底部 tab、统计页、报表页

**月度报表 (Monthly Report)**:
图表页的月度粒度视图：选择某月后展示该月支出/收入合计、本月分类排行与月内每日支出柱状。
_Avoid_: 月报

**年度报告 (Annual Report)**:
图表页的年度粒度视图：选择某年后展示全年合计、全年分类排行、12 个月支出趋势折线与年度高亮（记账笔数、支出最高月、单笔最大支出、日均支出）。TOP 类别不在高亮中——分类排行区块已覆盖。
_Avoid_: 年报

**分类排行 (Category Ranking)**:
按金额降序排列的类别条形列表，用于展示指定时间段内各类别支出的相对大小。
_Avoid_: 排行、分类占比

**趋势图 (Trend Chart)**:
时间序列可视化：月度粒度下是月内每日支出柱状，年度粒度下是 12 个月支出折线。
_Avoid_: 曲线图、走势图

**年度热力图 (Annual Heatmap)**:
年度报告中的只读 GitHub 风格日期网格：以一周为一列、星期为一行，周一为每周第一天，横向展示全年每日支出金额。网格从包含 1 月 1 日的周一开始，到包含 12 月 31 日的周日结束；年外日期为空白占位。月份标签显示在该月 1 日所在的周列上方。按当年非零支出的分位数分为四级支出红，并用“少 → 多”图例说明强度；不支持点击、日期选择、摘要或口径切换。
_Avoid_: 日历、贡献图



**热力图口径 (Heatmap Metric)**:
年度热力图的统计维度。支出金额按当天总支出统计，记账笔数按当天已入库账目数统计，记账天数表示当天是否至少确认一笔账目。金额与笔数按当年非零数据的分位数自适应分为四级；记账天数为有/无两级。支出金额使用支出红，记账笔数使用荧光青（主色），记账天数使用收入绿。图例用“少 → 多”的相对强度色块，不显示随年度变化的具体阈值；记账天数图例为“无 / 有”。
_Avoid_: 使用频率、活跃度

## OCR 记账

**OCR 记账 (OCR Entry)**:
通过相册截图触发的记账流程：用户在助手页选取支付宝/微信/云闪付截图，本地 ML Kit 提取文字，文字送 DeepSeek 解析，结果走与文字输入相同的确认卡片流程入库。v1 仅支持相册选图，不含相机实拍。
_Avoid_: 拍照记账（v1 无相机）、扫码记账

**OCR 输入 (OCR Input)**:
ML Kit 从截图中提取的原始文字，作为可记账输入送入解析流程。与用户手打的自然语言输入的区别仅在于来源；调用 ParseEntryUseCase 时以 `isOcrInput = true` 标记，系统提示中追加截图上下文。去除空白后不足 15 字符则视为识别失败，不进入解析。
_Avoid_: 识别文字、扫描结果

## 微信账单导入

**微信账单导入 (WeChat Bill Import)**:
设置页数据管理中的操作：读取微信支付导出的账单文件，把其中的收支流水恢复为账目入库。导入的账目与手动/AI/OCR 记账入库的账目没有区别，共用同一套账目模型。
_Avoid_: 账单同步、账单迁移

**来源引用 (Source Ref)**:
账目上携带的来源标识，微信导入的账目记录其交易单号。同一交易单号只能存在一条账目（去重依据）；也是"撤销本次导入"按集合定位账目的依据。手动/AI/OCR 记账的账目没有来源引用。
_Avoid_: 单号、流水号、导入标记

**导入确认 (Import Confirmation)**:
微信账单导入前的汇总确认层：展示账单时间范围、支出/收入/中性笔数、分类分布，以及去重预览（本文件将新增 vs 与已有账目重复）。确认后一次性入库，不逐笔确认。
_Avoid_: 导入预览、确认弹窗

**来源标签 (Source Tag)**:
微信导入的账目入库时统一挂上的「微信」标签，用于在账本页筛选微信来源的账目。标签在首次导入时自动创建（不导入则不出现），之后复用；撤销导入时标签关联随账目级联清除，标签本身保留在标签库可手动删除。
_Avoid_: 微信账单标签、导入标记

**关键词映射 (Keyword Mapping)**:
把微信账单行归入类别的内置规则：关键词匹配「交易类型 + 交易对方 + 商品」（交易类型纳入匹配是为了让"微信红包"这类行命中红包类别），只映射到已有类别，未命中落到「其他」。离线、结果确定，不依赖 AI。
_Avoid_: AI 分类、智能分类

**中性 (Neutral)**:
账目类型的第三类，与支出、收入并列。中性记录真实入库、出现在账本页，但不计入任何统计——今日合计、日历页金额与绿点、图表页全部排除。目前仅微信账单导入产生中性记录（退款交易），手动/AI/OCR 记账无法创建。类别用保留名「中性」占位，不参与分类排行。
_Avoid_: 不计收支、中性交易（那是微信账单中的另一概念）

**中性交易 (Neutral Transaction)**:
微信账单中充值、提现、理财通购买、零钱通存取、信用卡还款等账户间转账，不是收入也不是支出。导入时跳过，仅在导入结果中报告跳过笔数。
_Avoid_: 转账记录（与用户间转账混淆）

**退款交易 (Refund Transaction)**:
微信账单中已退款消费与其对应的退款流水。不标记收入也不标记支出，消费与退款两行都作为中性记录入库——消费行保留"买过什么"，退款行保留"退了多少钱"，真实反映完整经过。导入结果中正常报告笔数。
_Avoid_: 退款收入、跳过退款

## 桌面小组件

**收支速览 (Overview Widget)**:
桌面小组件，小（2×1）与中（4×2）两档尺寸：小展示今日支出与今日收入，中展示今日支出、今日收入、本月支出、本月收入。深色卡片沿用 App 视觉身份（暗色独占 + 支出红/收入绿；Glance 1.1 不支持打包字体，widget 文本用系统默认字体）。点击打开 App 首页。
_Avoid_: 桌面挂件、快捷查看

**小组件口径 (Widget Metric)**:
小组件上数字的统计语义：今日 = 日期为今天的账目合计（含补记到今天），本月 = 当月 1 日至月末的账目合计。与 App 内今日合计、日历/图表月度口径一致，中性记录不计入——任何界面看到的数字互相咬合。
_Avoid_: 独立统计、另算口径

## 自动记账

**自动记账 (Auto Entry)**:
设置页可开关的功能：开启后，微信/支付宝的支付通知到达时自动解析为账目草稿，用户确认后入库。v1 只监听微信与支付宝两个包名，支出与收入双向都记。
_Avoid_: 通知记账（那是机制名）、智能记账

**通知记账 (Notification Entry)**:
自动记账的触发机制：通过通知监听读取支付通知文本，把通知作为记账输入源——与手打文字、OCR 输入并列的第三种输入源。用户侧不出现这个词，见「自动记账」。
_Avoid_: 自动记账（那是功能名）

**支付通知 (Payment Notification)**:
微信/支付宝发出的、含金额与商户/对方的到账通知，通知记账的输入。收入方向含「你已收款」「收到红包/转账」类通知；微信「你收到一笔转账」是待收款（可能退回）而非已到账，解析时需区分。
_Avoid_: 交易提醒、扣款通知

**通知监听 (Notification Listening)**:
通过系统 NotificationListenerService 读取通知文本的机制。只读通知文本、不读屏幕内容；按包名白名单过滤，其余通知立即丢弃；系统授权入口为 `Settings.ACTION_NOTIFICATION_LISTENER_SETTINGS`，与无障碍服务是两条不同的权限。
_Avoid_: 无障碍、通知读取

**通知闸门 (Notification Gate)**:
通知进入解析前的离线确定性过滤：包名白名单（微信/支付宝）+ 通知文本含金额/关键词模式。闸门不过的静默丢弃、不进解析也不打扰用户——解析失败的低姿态提示只针对闸门过了的情况。
_Avoid_: 噪音过滤、智能判断

**待确认草稿 (Pending Draft)**:
通知记账解析成功但用户尚未确认的账目草稿。确认通知（App 自己发的那条）与 App 内待确认队列都展示它；确认后按确认卡片流程入库。不点确认通知不丢——草稿留在队列里。
_Avoid_: 未确认账目、挂起卡

## 设计语言

**终端主题 (Terminal Theme)**:
品牌视觉为终端黑客风：深黑底 + 荧光青主色（terminal cyan）+ Matrix 荧光绿/电紫点睛，等宽字体（数字、符号、拉丁呈控制台观感，中文字形回退系统默认），锐利硬边的小圆角。暗色独占、不使用动态取色。见 ADR 0019。
_Avoid_: 蜂蜜金、暖调、圆润

**语义角色 (Semantic Role)**:
Material 3 颜色体系中的角色（primary、surface、error 等）。组件只消费语义角色、不直接引用具体色值，主题换色时组件无需改动。
_Avoid_: 颜色、色值

**领域语义色 (Domain Semantic Color)**:
为记账领域语义设立的独立颜色 token（支出红、收入绿），不借用语义角色——尤其不与 error 混淆，错误提示与支出金额在视觉上必须能区分。
_Avoid_: 状态色、金额色

**暗色独占 (Dark-Only)**:
应用固定深色外观，不跟随系统明暗，也不使用动态取色。记账助手多用于夜间场景，深色是产品主张而非可选项。
_Avoid_: 夜间模式、深色模式开关

**形状 token (Shape Token)**:
`BeeCountShapes`（ui.theme.Shape.kt）定义 5 级圆角——extraSmall 12dp、small 16dp、medium 20dp、large 28dp、extraLarge 32dp。组件优先引用 shape token 而非硬编码 `RoundedCornerShape(x.dp)`；图表内部微圆角（2-6dp）属绘图细节，保留原位。
_Avoid_: 圆角值、corner radius

**间距 token (Spacing Token)**:
`Spacing`（ui.theme.Spacing.kt）基于 4dp 网格提供常用间距（xxs 4dp → huge 48dp）。页面 padding、Spacer 高度、`Arrangement.spacedBy` 优先引用 `Spacing`，避免散落魔法数字。
_Avoid_: dp 值、间距常量

**尺寸 token (Dimens Token)**:
`Dimens`（ui.theme.Dimens.kt）集中图标大小、最小触控高度、图表辅助尺寸等不隶属于间距/形状的尺寸。
_Avoid_: 字体大小（归 Typography）、间距（归 Spacing）

**组件默认 token (ComponentDefaults)**:
`ComponentDefaults`（ui.theme.ComponentDefaults.kt）为常用组件（Card、底部导航胶囊）集中提供默认样式——容器色、形状、阴影等。页面不直接写 `CardDefaults.cardColors(containerColor = ...)`，而是引用这里的语义化常量与函数。
_Avoid_: 组件默认值散落各处
