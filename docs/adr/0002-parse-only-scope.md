# 首版范围为纯录入解析器

AI 小助手第一版只做"自然语言 → 解析为账目 → 确认卡片 → 入库"这一条录入链路，明确排除查账问答（"上个月吃饭花了多少"）、月度统计、完整账本页和独立类别管理页。这是刻意的边界：查账与统计依赖账目数据的聚合能力，应在录入链路稳定、Room 数据积累后作为第二阶段实现；首版仅保留"今日已记"列表和"今日合计"作为录入闭环的反馈，供后续统计复用同一套聚合逻辑。
