# 设置页：第三 tab + CSV 导出 + 清空范围

设置从顶栏文字按钮 + API Key 弹窗升级为独立的第三个底部 tab，承载 API Key 管理、类别/标签管理入口、数据管理与关于。两个范围决策需要记录：数据导出用 CSV（Excel/WPS 可直接打开）经系统分享保存，不做自有文件格式，也不做导入（导入是未来能力）；清空账目**只清账目，保留类别与标签**——它们是持续使用的元数据，清账目是「清零流水」而非「恢复出厂」。
