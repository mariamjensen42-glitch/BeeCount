# 标签系统：标签库 + 关联表，AI 自动打标，交集筛选

标签系统采用「预定义 + 自定义」：SQL 种子一批常用标签并配好颜色，确认卡片上可新建标签、首次出现自动注册并按 8 色板顺序取色，颜色可改且全局生效。数据模型为 Tag 表（name/color/isCustom）+ 账目-标签关联表（多对多），而非账目上冗余标签快照——快照方案改色不追溯旧账、筛选只能 LIKE 模糊匹配，长期是坑。AI 解析时自动从标签库选 1~3 个标签附在结果上（库外新标签自动注册），确认卡片可增删改。筛选放在账本页顶部，多选为交集（AND），不持久化。
