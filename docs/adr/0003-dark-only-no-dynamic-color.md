# 暗色独占，关闭动态取色

BeeCount 固定深色外观：不跟随系统明暗、不提供亮色模式、显式关闭 Android 12+ 动态取色。记账助手多用于夜间/睡前场景，深色是产品主张而非可选项，因此 token 体系只维护一套暗色语义 token，启动主题也直接配深色、不闪白。动态取色默认开启、会把品牌色按壁纸覆盖掉，与"品牌色一致"冲突，故显式关闭——若未来某天它又被打开，那大概率是有人误以为它是标准配置，而非设计变更。
