# 蜂蜜金品牌色板与独立收支语义色

品牌色板取蜂蜜金（琥珀金 primary，暖黑底），呼应 BeeCount 的蜜蜂意象，替代 Material 模板紫色。支出/收入使用独立的领域语义色（亮红/亮绿），而非借用 M3 的 error/primary 角色：借贷 error 会让"错误提示"与"支出金额"同色产生歧义，借贷 primary 会让"收入"与品牌强调色混淆。领域色独立于语义角色，是 token 体系的显式一层。
